# autonomous/

Logs from autonomous skill runs. Created by `infra/auto-skill.sh`. One file per run: `YYYY-MM-DD-HHMM-skill-name.md`.

Each log includes: what ran, the output, exit code, and notification status.
