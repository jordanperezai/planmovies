## Session 18 — 2026-05-30

### What was done
End-to-end landing redesign + brand correctness pass + UX polish. Four commits shipped to planmovies.com.

**Brand corrections:**
- Color corrected from #d9480f (Session 16's wrong label) to **#e53908** (traced from the brand sheet pixels). Jordan caught it visually.
- Mark geometry re-traced from brand sheet pixels: chunky rounded-top TABS (viewBox 216×36, W=40/gap=4/r=12). Session 16's 154×88 skinny-finger version was wrong.
- Nav: mark removed from nav — now **wordmark only** (`planmovies`). Jordan's call.

**Landing redesign (`be1268f`):**
- Left-aligned hero on one spine (lockup, poster, crew avatars, title, date, countdown, CTA).
- Slogan "Get the row together." added under the wordmark.
- Date now prominent in cream + venue line (Regal Secaucus · Secaucus, NJ).
- `100dvh`-safe: `.landing-inner{flex:1;min-height:0}` + `justify-content:safe center`.
- Fill system: open seats = ember outline, filled = solid ember. `renderHeroRow()` hooked to `updateLandingHeadcount`.

**Critique fixes (`e271f18`) — 30/40 baseline, /critique snapshot saved:**
- P0: open seats were 2.19:1 (nearly invisible). Changed dim-fill to ember **OUTLINE** — fixes both contrast and battery/seats ambiguity. Logo Rangers originally recommended this; the dim-fill was my override; critique vindicated them.
- P1: fill logic honesty — 0 RSVPs now shows all-outline + "Be the first"; >5 shows "The row is set · N going."
- P1: ember monochrome wash — title + countdown → cream, credit/social/caption → neutral. Ember reserved for mark + "movies" + CTA.
- P1: "What is Disclosure Day?" touch target bumped to 44px.
- "DIRECTOR" label stays for now; HOST rename logged as a future improvement.

**Nav + overlap fix (`3f878b4`):**
- Crew avatars lived inside `flex:1; min-height:0` carousel. At tall viewports (iPhone Pro), carousel shrank, avatars overflowed and collided with the title. Fixed by moving the crew-dots div into normal flow below the carousel.
- Nav SVG was `width:auto` → fell back to 216px viewBox width → hogged nav space. Fixed with explicit `width:78px`.

**Seat-fill takeover (`a0534d6`):**
- Full-screen overlay on RSVP (going/probably/maybe): row fills ember to the live count, your seat pulses brighter, "You're in, / [Name]" lands, then hands off to the inline confirmation.
- ~1.5s, tap-to-skip, reduced-motion aware, honest at all edges.
- The peak-end moment the /critique said was missing + the loud "you're in" confirmation Carmen and Ray needed.

**Idea 2 — World+sheet concept (`logos/_review/world-sheet-mock.html`):**
- Mockup only (not shipped). World of Disclosure Day full-screen behind a peeking bottom sheet. Jordan's idea, validated by mock. Verdict: strong concept, lives or dies on the drag feel. Worth building on the crew/dashboard page first.

### Key decisions
- The Logo Rangers' outline recommendation was correct all along. The dim-fill override in Session 17 failed contrast. Overrides of panel consensus need stronger evidence.
- Ember is punctuation, not wallpaper. Reserve full #e53908 for the mark, "movies," and the CTA exclusively.
- The seat-fill takeover > any inline confirmation because it's personal and felt, not read.
- World+sheet is a real architectural shift; prototype on crew page before committing to landing.

### What's deployed
All 4 commits live on planmovies.com. Verified.

### What's next
1. Push the world-sheet idea forward (build real draggable version, crew page first).
2. Final lit logo art: Imagen → rembg → app-icon/og:image.
3. Jordan: TMDB API key, Twilio Verify.
4. Send to family — **13 days out** (Jun 12).
5. Louder post-RSVP screen confirmation done (seat-fill). Next: share prompt moment.

### What's blocked
- og:image share card needs raster logo art (rembg pipeline not yet run).
- TMDB + Twilio are Jordan's tasks.
## Session 19 — 2026-05-30

### What Was Done
Public launch pivot + security hardening + landing redesign shipped to local. The biggest session yet in terms of scope: product strategy, live DB security, and a full hero composition change, all in one session.

**Launch pivot (strategy):**
- Decided to launch PlanMovies PUBLICLY on June 1 2026 (two days away). Scope C: let anyone create their own Disclosure Day crew in their city. Per-city crews, shared movie-world content, no TMDB/any-movie picker at launch.
- PlanMovies is the brand and the business. Disclosure Day is the flagship launch event, not the ceiling.
- Framework decision: stay single-HTML. Static on Cloudflare CDN is the scaling advantage for a spike. Astro is the future migration if needed; Next is never the answer.
- Phone verification: on for launch, with Twilio spend cap + Fraud Guard + geo-lock.

**Security hardening (live in Supabase):**
- Ran the security advisor: 12 wide-open write policies + public-listable photo bucket. Confirmed with direct pg_policies query.
- Applied Phase 1 migration live: dropped "Payments/Organizers/Events full access for testing" policies. Added "Events are publicly readable" (SELECT only). payments + organizers are now fully private. Workers use service_role key and are unaffected.
- 6 XSS sinks fixed in index.html: all photo_url and one_movie_poster src attributes now run through escAttr(). A 6th sink (profile one_movie_poster) was found that the security workflow missed.
- Security advisor went from 12 write WARNs to 9 (payments/organizers/events cleaned; rsvps/seat_assignments remain — wired with auth ownership migration before launch).

**Landing redesign (local, not yet deployed):**
- Critique workflow: 23/40 mobile score. Root cause confirmed: .poster-carousel-track collapsed to 150px min-height; poster was a thumbnail, not the hero.
- Rebuilt landing as Path A: poster is full-bleed backdrop with bottom-up scrim; content bottom-anchored in lower third; space-between layout (lockup top, lower zone bottom).
- "Now Planning" eyebrow added. Title demoted to text-2xl. Crew avatars relocated into lower zone with DIRECTOR badge contained.
- Lit-deep gradient (#ff6326→#e53908) applied to: mark filled seats (SVG linearGradient#ember-vert), "movies" wordmark (background-clip:text), CTA button. Contrast verified: 4.69:1 at base, 6.74:1 at bright top — both AA. Clamped deep end at base #e53908 (pushing to #b32c06 fails at 3.13:1).
- renderLandingCrewDots() replaced by renderLandingCrew() targeting #landing-crew-row in the lower zone.
- Poster resolution bumped from w500 to w780 for backdrop quality.
- Gradient temperature comparison built: logos/_review/gradient-temps.html. Jordan has not yet picked a temperature (primary is live, warmer and lit-cool are alternatives).
- Mock file: logos/_review/landing-v2-mobile.html.
- Rendered at 390, 430, 1280 — all verified. **NOT YET COMMITTED OR DEPLOYED.**

**Twilio setup (Jordan's side):**
- Created Twilio account (trial), picked Verify as the product.
- Created "PlanMovies" Verify Service with Fraud Guard on. Service SID generated (VA…).
- Geo permissions set to US + Canada SMS only (caught Voice-only bug before saving — would have blocked all SMS codes).
- Geo saved correctly: Canada SMS, United States SMS.
- **Still pending:** upgrade out of trial, paste Account SID + Auth Token + Service SID into Supabase Auth → Providers → Phone → Twilio Verify.

**Memory saved:**
- project_launch-pivot-public-platform.md (durable platform/launch strategy memory).

### Key Decisions
- Single HTML file stays. Scale-proof on Cloudflare CDN. Astro when per-crew link previews + SEO become the growth engine.
- Scope C for June 1: one movie (Disclosure Day), many crews, per-city. No TMDB, no payments day one.
- Poster as full-bleed backdrop is correct reading of .impeccable.md:50. The 150px thumbnail was a CSS collision (max-height:44vh on the img vs min-height:150px on the track).
- Lit-deep gradient (not flat solid) is the right move: improves the contrast margin AND calms the monochrome wash simultaneously.
- Do not pay for new Twilio number. Twilio Verify sends from managed pool. No number purchase needed.
- Twilio credentials live in Supabase dashboard only, never in .env or index.html.

### What's Deployed
Phase 1 DB lockdown (live in Supabase) — confirmed via pg_policies + get_advisors.
All Session 18 work remains live on planmovies.com.
index.html changes: local only, uncommitted.

### What's Next
1. Jordan: upgrade Twilio trial → paste 3 values into Supabase Auth → Phone → Twilio Verify.
2. Jordan: pick gradient temperature (1=primary/lit-deep, 2=warm/burnt-orange, 3=lit-cool) from logos/_review/gradient-temps.html.
3. Commit + deploy the landing rebuild once gradient is picked.
4. rsvps ownership lockdown migration (drop public DELETE/UPDATE, scope to auth.uid()) — gate before Reddit post.
5. Multi-tenant wiring: event_id into fetchAttendees/submitRSVP/realtime; create-crew form; seed city events.
6. Reddit post draft.
7. Jordan: Supabase Pro upgrade ($25/mo) for launch headroom.

---

## Session 20 — 2026-05-30

### What Was Done
Continuation past the Session 19 wrap-up. Twilio finished (Jordan's side), the phone-OTP RSVP gate built and verified, a Codex adversarial review that caught a LIVE P0, the bug-logging scaffold gap diagnosed and fixed at the source, and the brand refined (gradient picked, hero centered, empty seats greyed). Nothing deployed; live DB still only read.

**Twilio (Jordan finished his side):**
- Upgraded out of trial ($20 starting balance + auto-recharge on). Caught a setup misstep: Supabase Phone provider was set to plain "Twilio" with the Verify Service SID (VA…) in the Message Service SID field. Switched provider to "Twilio Verify" so the VA SID + Fraud Guard + geo-lock actually apply. Phone provider enabled and saved.
- Renamed the Supabase project disclosure-day → planmovies (cosmetic; ref/keys unchanged).

**Phone-OTP gate (built, local, verified):**
- The live RSVP flow had NO phone gate — a prior session left orphaned OTP functions with no UI wired to them. Built it from scratch: a `#auth-gate` modal (phone → 6-digit code) firing at COMMIT (submitRSVP → requireAuth → resumeRSVP), preserving social-proof-first. Returning verified users skip it (boot getSession + onAuthStateChange). createClient given explicit auth options.
- submitRSVP now stamps `user_id`; removed the 23505 name-overwrite hole.
- Organizer model = "phone login, you're admin" (Jordan's choice): togglePaid/toggleTicket/removeAttendee/manualAddRsvp/assignSeat/unassignSeat each gated via `requireAuth(..., 'organizer')`; authorization enforced in the DB via `auth.uid() = user_id OR is_organizer()`.
- Rendered both gate steps + verified JS syntax clean. Modal on-brand (dark card, ember CTA, mono OTP).

**Codex adversarial review (two-brain):**
- Ran FOREGROUND (after a process miss — see LEARNINGS #12). Found a LIVE P0 the earlier Claude-only pass missed: activity_feed stored XSS — the `<strong>`-allowlist regex permits event-handler attributes (`<strong onmouseover=…>`) and the result is rendered as innerHTML; the insert policy is open to anon. PATCHED (escape-all then restore bare `<strong>`).
- Also surfaced + triaged: self-set paid/ticket_secured (P1, deferred); unbounded RSVP rows (P1, deferred — REJECTED Codex's UNIQUE(user_id) fix, it breaks "RSVP for someone else"); ticket-monitor worker key uncertainty (P1, deferred — pulled ticket_status OUT of the lockdown); double-tap + silent-success (P2, FIXED). is_organizer escalation / optimistic-UI / unique-name: checked, fine.

**Migration prepared (NOT applied):**
- `migrations/2026-05-30_phase2_rsvp_lockdown.sql` + rollback. organizer_admins table + is_organizer() SECURITY DEFINER. Locks rsvps (owner-or-organizer), seat_assignments (organizer write), activity_feed (authenticated insert). ticket_status DEFERRED. Live policy names verified via pg_policies. Atomicity note added. Apply LAST in the deploy sequence.

**Bug-logging scaffold fixed (root cause + source fix):**
- bugs/ had been empty since day 1: logging was wrap-up-only, and compaction eats chat-only bugs (especially deferred ones). Fixed with log-on-discovery rule (bugs/CLAUDE.md + log-bug SKILL.md) + pre-compact hook now nudges for unlogged bugs. Logged this session's 4 bugs. PORTED the fix UP to Zordon (bugs/CLAUDE.md, pre-compact-nudge.sh, log-bug skill) so future morphs inherit it. The Codex-foreground rule went to global ~/.claude/rules/platform-gotchas.md (cross-project tool fact).

**Brand:**
- Gradient temperature #1 (lit-deep) LOCKED over warm/lit-cool. Recorded in .impeccable.md.
- Hero CENTERED (nav stays left) — Jordan's "you're seated mid-theater" read. Landing CSS, rendered/verified, no clipping.
- Empty seats → GREY outline (#6b7078, 4.02:1) instead of ember outline, on the landing mark AND the seat-fill takeover. Makes ember exclusively mean "a person"; the grey→ember stroke transition is the row lighting up. Decided via a contrast-tested empty-seat board (logos/_review/empty-seat.html).
- No recliner/seam detail — verified it dies at favicon size (logos/_review/seat-styles.html). Favicon stays flat solid.

### Key Decisions
- Organizer access after lockdown = phone login + organizer_admins allowlist via is_organizer(), NOT a service-key Worker route. Keeps the single file.
- Empty seat = GREY outline, not ember. Ember = a filled seat = a person.
- Hero centered for the landing; nav lockup stays left (they don't have to match).
- ticket_status lockdown deferred until the ticket-monitor worker key is confirmed service_role.
- Run agents FOREGROUND when you need their output (LEARNINGS #12).
- Bugs get logged at discovery, not wrap-up (scaffold fixed in Zordon).

### What's Deployed
Nothing new. All Session 20 work is local/uncommitted. Live DB only READ (policy names verified, not changed). Session 19's Phase 1 lockdown still live.

### What's Next (deploy sequence)
1. Commit + push → Cloudflare deploys → confirm planmovies.com live.
2. Jordan RSVPs on the LIVE site with his phone → real OTP round-trip test (while old permissive policies still live, so failures are loud).
3. Apply phase2 migration → insert Jordan's verified uid into organizer_admins → re-run get_advisors → confirm an anon DELETE is rejected.
4. Delete the 3 legacy seed rows (Jordan / Sarai Perez / Taylor).
5. Deferred (logged in bugs/): paid-column guard (before payments), per-user RSVP cap, ticket_status lockdown (after worker key).
6. Still pending from S19: multi-tenant event_id wiring, Reddit post draft, Supabase Pro upgrade.

### What's Blocked
- Landing deployment: waiting on Jordan's gradient temperature pick.
- Phone auth in RSVP flow: waiting on Twilio → Supabase Auth config (Jordan's task).
- rsvps lockdown: depends on phone auth being live (ownership = auth.uid()).
- Multi-tenant: safe to build but can't gate launch until rsvps lockdown + auth are done.

